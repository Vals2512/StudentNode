package model;

/**
 * Enum representing the action types that can be performed when adding a
 * student.
 */
public enum ActionType {
    ADD,
    ADD_FIRST,
    ADD_LAST,
    ADD_BEFORE,
    ADD_AFTER
}
